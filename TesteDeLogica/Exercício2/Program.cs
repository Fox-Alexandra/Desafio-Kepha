using System;

namespace Exercício2
{
    class Program
    {
        static void Main(string[] args)
        {
            //Encontre o menor valor positivo inteiro que não pode ser representado como uma soma de um subconjunto em um array.
            // Dado um array ordenado (ordem crescente) de números positivos, encontre o menor valor positivo inteiro que não pode ser representado como a soma de elementos de um subconjunto deste array.
            // Complexidade experada é 0(n).
            // Exemplos:
            // Input:  arr[] = {1, 3, 6, 10, 11, 15};
            // Output: 2
            Console.WriteLine("Hello World!");
        }
    }
}
